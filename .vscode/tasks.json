{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "DynamoDB - InMem",
      "type": "shell",
      "command": "dynamodb-local -inMemory -sharedDb -delayTransientStatuses"
    },
    {
      "label": "DynamoDB",
      "type": "shell",
      "command": "dynamodb-local -sharedDb -delayTransientStatuses"
    }
  ]
}